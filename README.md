# Python Learning Foundations 🚀

Welcome to my repository tracking my transition from core logic building to job-ready Python development. This space serves as an open log of my projects, challenges, and breakthroughs.

## 📁 Repository Contents

* **[Habit Tracker](./habit_tracker/):** An XP/RPG-style gamified terminal tool practicing persistent data saving, data loops, and streak logic.
* **[Core Logic & Architecture Challenges](./core_logic_and_algorithms/core_logic.py):** A unified suite of functions focusing on algorithmic loops, mathematical structures, set operations, bitwise access management, and intermediate exception handling.
* **[PokéAPI Program](./PokeAPI.py):** Practice with HTTP requests, handling status codes, and parsing nested JSON data.

## 🧠 Mastered Concepts & Breakthroughs

| Topic / Function | Concept Mastered | The Breakthrough |
| --- | --- | --- |
| `find_min` | Edge Case Handling | Utilizing `float('inf')` to handle inputs cleanly against an absolute baseline. |
| `area_sum` | Nested Structure Loops | Iterating through ranges to safely calculate properties inside a list of dictionaries. |
| `factorial` | Accumulator Loops | Implementing step-by-step multiplication ranges using the `*=` operator. |
| `remove_nonints` | Safe Sequence Iteration | Avoiding the mutation/shrinking of a list mid-loop by populating a separate, isolated tracking list. |
| `number_sum` | Arithmetic Progressions | Utilizing inclusive bounds (`n + 1`) to accurately aggregate incremental values. |
| `@universal_zero_guard` | Metaprogramming & Decorators | Using an outer configuration function and an inner `wrapper` to hijack input streams (`*args`, `**kwargs`) and short-circuit potential crashes before execution. |
| `filter_messages` | Parallel Sequences & Tokenization | Splitting raw strings into individual word arrays while simultaneously tracking secondary metric arrays to build synchronized application telemetry. |
| `binary_string_to_int` | Dynamic Radix Interpretation | Utilizing custom base-2 conversion protocols `int(x, 2)` to parse human-readable binary strings into native Python integers. |
| `calculate_guild_perms` | Low-Level Masking | Deploying binary prefixes (`0b`) and bitwise `\|` (OR) / `&` (AND) operations to construct fast, memory-efficient authorization systems. |
| `calculate_dps` | Numeric Delimiters | Implementing Pythonic visual delimiters (`_`) to manage high-magnitude integers cleanly without introducing string parsing overhead. |
| `check_ingredient_match` | Cross-Sequence Analysis | Comparing separate list sequences using the `in` keyword to track missing array values and calculate accurate floating-point metrics. |
| `get_most_common_enemy` | Hash Map Parsing | Iterating across dictionary key-value records (`.items()`) to resolve a maximum occurrence string key tracking against a negative infinity (`float('-inf')`) baseline. |
| `get_quest_status` | Nested Key Pathway Resolution | Accessing specific values inside multi-tiered, deeply nested dictionary objects via sequential key chaining without using processing-heavy loops. |
| `find_missing_ids` | High-Efficiency Set Difference | Utilizing set subtraction (`-`) to perform instant sequence exclusions in a single line of code, ensuring unique outputs while avoiding nested loop dependencies. |
| `get_player_record` | Defensive Programming | Rejecting "bare except" habits by explicitly raising targeted system errors (`raise IndexError`) and catching them specifically (`except IndexError as error`) to prevent silent failures. |
| `poke_api.py` | Nested Data Parsing | Accessing deep dictionary pathways like `element['type']['name']`. |
| `.json()` Method | Data Serialization | Translating raw text strings from the web into manipulable Python dictionaries. |
